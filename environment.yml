name: limitlesstcg #Environment name
dependencies:
  - python=3.10
  - pip:
    - dash
    - dash-renderer
    - dash-core-components
    - dash-html-components
    - plotly
    - gunicorn # for app deployment